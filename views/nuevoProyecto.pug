extends layout

include mixins/crearProyecto
block container
    .contenido-principal
        h1 #{nombre}
        if errors
            each error in errors
                .alerta #{error.texto}
        +crearProyecto(proyecto)

