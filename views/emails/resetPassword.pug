doctype html
html(lang="en")
    head
        meta(charset="UTF-8")
        meta(http-equiv="X-UA-Compatible", content="IE=edge")
        meta(name="viewport", content="width=device-width, initial-scale=1.0")
        style
            include ./email.css
    body
        h2 Reestablecer password
        p Hola, has click en el siguiente enlace para reestablecer tu contraseña
        a(href=`${resetUrl}`) Ir a reseat la contraseña
        p Si no puedes acceder a este enlace, visita #{resetUrl}
        p Si no solicitaste este email, ignoralo