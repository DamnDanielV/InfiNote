doctype html
html(lang="en")
    include partials/head
    body.login
        .contenedor-formulario
            h1= nombre
            if locals.errores
                .alertas
                    each categoria in Object.keys(locals.errores)
                        each error in errores[categoria]
                            .alerta(class=`${categoria}`)
                                p= error 
            p Indica tu E-mail
            form.caja-login(method="POST" action="/reestablecer")
                .campo
                    label(for="email") E-mail:
                    input(type="email" name="email" placeholder="Tu email" value= email)
                .campo.enviar
                    input.boton(type="submit" value="Reestablecer")
                .campo.acciones
                    a(href="/iniciarSesion") Volver a login