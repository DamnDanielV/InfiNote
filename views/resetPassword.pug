doctype html
html(lang="en")
    include partials/head
    body.login
        .contenedor-formulario
            h1= nombre
            p Indica tu nueva contraseña
            form.caja-login(method="POST")
                .campo
                    label(for="password") Contraseña:
                    input(type="password" name="password" placeholder="Tu nueva contraseña" value= password)
                .campo.enviar
                    input.boton(type="submit" value="Reestablecer Contraseña")